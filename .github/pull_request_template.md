## What this changes

## Why

## How it was tested
- [ ] `npx tsc --noEmit` passes in `server/`
- [ ] `npx tsc --noEmit` and `npx vite build` pass in `web/`
- [ ] Ran the stack locally and exercised the change in the browser
- [ ] Checked it in light, dark, eye-comfort and midnight modes (for UI changes)

## Screenshots
_For anything visual, before and after._
